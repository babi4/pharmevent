= simple_form_for(@user) do |f|
  = f.error_notification

  .form-inputs
    = f.input :name
    = f.input :email
    = f.input :password
    = f.association :roles, :as => :radio_buttons

  .form-actions
    = f.button :submit
