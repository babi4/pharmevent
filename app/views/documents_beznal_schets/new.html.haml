%ul.breadcrumb
  %li
    = link_to 'Мероприятия', events_path
    %span.divider /
  %li
    = link_to @event.name, @event
    %span.divider /
  %li.active Новый безналичный счет

= render 'form'

- content_for :title do
  #{@event.name} - новый безналичный счет