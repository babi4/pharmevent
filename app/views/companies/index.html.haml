%h1 Listing companies

%table
  %tr
    %th Company members
    %th Events
    %th Name
    %th Company type
    %th Short name
    %th
    %th
    %th

  - @companies.includes(:events).each do |company|
    %tr
      %td= link_to 'Company members', company_company_members_path(company)
      %td= company.events.pluck(:name)
      %td= company.name
      %td= company.company_type
      %td= company.short_name
      %td= link_to 'Show', company
      %td= link_to 'Edit', edit_company_path(company)
      %td= link_to 'Destroy', company, method: :delete, data: { confirm: 'Are you sure?' }

%br

= link_to 'New Company', new_company_path
