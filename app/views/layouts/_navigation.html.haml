= link_to "Pharm Event", root_path, :class => 'brand'
%ul.nav
  - if user_signed_in?
    %li
      = link_to 'Выйти', destroy_user_session_path, :method=>'delete'
    - if can? :manage, User
      %li
        = link_to 'Админ', users_path
  - else
    %li
      = link_to 'Войти', new_user_session_path
