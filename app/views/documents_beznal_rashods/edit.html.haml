%ul.breadcrumb
  %li
    = link_to 'Мероприятия', events_path
    %span.divider /
  %li
    = link_to @event.name, @event
    %span.divider /
  %li.active Редактирование безналичного расхода

= render 'form'

- content_for :title do
  #{@event.name} - редактирование безналичного расхода